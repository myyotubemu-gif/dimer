{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for PUBG Drop Pro",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma db push --accept-data-loss && node seed.js && node server.js",
    "dev": "nodemon server.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0"
  }
}
